import React from 'react';

import './OpenSource.css';

const OpenSource = () => {
  return (
    <div className="OpenSource">
      <h1>OPEN SOURCE</h1>
    </div>
  );
};

export default OpenSource;
